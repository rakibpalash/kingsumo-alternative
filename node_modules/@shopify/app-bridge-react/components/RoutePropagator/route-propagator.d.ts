import type { ClientApplication } from '@shopify/app-bridge';
export type LocationOrHref = string | {
    search: string;
    hash: string;
    pathname: string;
};
export declare function updateHistory(app: ClientApplication, location: LocationOrHref): Promise<void>;
